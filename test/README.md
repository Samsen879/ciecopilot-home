# Deprecated Test Directory

All active tests have moved to `tests/`.

Rules:

1. Do not add new automated tests under `test/`.
2. Keep this directory only as a legacy migration marker.
3. Active Python and JS test assets belong under `tests/`.
4. Manual fixtures formerly stored at the root of `test/` now belong under `tests/manual/`.

